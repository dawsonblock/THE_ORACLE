# Approval matrix

Suggested defaults:
- read-only retrieval: no approval
- proposal generation: no approval
- validation commands: no approval if allowlisted
- patch apply: always approval outside test mode
- high-risk tools: disabled unless explicitly enabled
