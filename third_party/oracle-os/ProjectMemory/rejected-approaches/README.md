# Rejected Approaches

Document approaches that failed or were explicitly rejected so the system does not repeat them blindly.

Each entry should include the reason for rejection and the evidence that led to it.
