{
  "profile": "default",
  "language": "default",
  "description": "Fallback validation profile",
  "stages": [
    {
      "id": "repo_sanity",
      "name": "Repository sanity",
      "commands": ["git status --short"]
    }
  ]
}
